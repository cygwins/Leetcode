/*
 * pair up in two, merge two, repeat
 */
#include <iostream>
#include <vector>
#include "catch.hpp"
using std::vector;

class Solution {
public:
    ListNode* mergeKLists(vector<ListNode*>& lists) {

    }
};

TEST_CASE("processing", "[methodName]") {
    Solution s;
    CHECK(s.methodName() == vector<int>{2,3});
}
